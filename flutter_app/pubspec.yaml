name: ione_vpn
description: IONE VPN – Secure, fast, cross-platform VPN client
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # State management
  provider: ^6.1.2

  # HTTP & API
  dio: ^5.4.3
  retrofit: ^4.1.0

  # Secure storage (tokens, config)
  flutter_secure_storage: ^9.0.0

  # WireGuard / VPN tunnel
  wireguard_flutter: ^1.0.3

  # UI
  cupertino_icons: ^1.0.6
  google_fonts: ^6.2.1
  flutter_svg: ^2.0.10+1
  lottie: ^3.1.0
  fl_chart: ^0.67.0        # speed/data charts
  country_flags: ^3.0.1
  shimmer: ^3.0.0

  # Utilities
  shared_preferences: ^2.2.3
  intl: ^0.19.0
  package_info_plus: ^8.0.0
  connectivity_plus: ^6.0.3
  url_launcher: ^6.2.5
  uuid: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.9

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/animations/

  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-Regular.ttf
        - asset: assets/fonts/Inter-Medium.ttf
          weight: 500
        - asset: assets/fonts/Inter-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Inter-Bold.ttf
          weight: 700
